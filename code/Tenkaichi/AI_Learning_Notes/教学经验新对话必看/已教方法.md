# 已教方法统计表

> **用途**：登记教学中**已经给用户讲过**的方法/函数/宏/关键字，避免重复教、也避免漏教（配合 [18_每次教学遇到没教过的方法都要教一遍](./18_每次教学遇到没教过的方法都要教一遍.md)）。
>
> **用法**：教一个新方法前先查本表——教过就一句话带过，没教就先教再登记。

---

## 一、统计字段说明

| 字段 | 含义 |
|------|------|
| 方法/宏/关键字 | 符号名 |
| 所属类/头文件 | 它属于哪个类、在哪个头文件 |
| 首次出现课次 | 第一次在教学代码里用到它的课次 |
| 一句话作用 | 它是干嘛的 |
| 详解 md | 若单独建了疑问解答 md，指向它 |

---

## 二、已教清单（按课次）

### 第 02 课 · 第 2 步（属性集基类 + 血量集）

| 方法/宏 | 所属 | 作用 | 详解 md |
|---------|------|------|---------|
| `CreateDefaultSubobject` | `FObjectInitializer` / `UObject` | 构造函数里造子对象组件并挂上 | Q04a |
| `NewObject` | `UObject` | 运行时动态创建对象（对比 CreateDefaultSubobject） | Q04b |
| 构造函数（谁调用/何时触发） | —— | 对象构造时机 | Q04 |
| 空构造函数为何还要写 | —— | 声明构造以支持 ObjectInitializer 等 | Q04c |
| `.h` 声明 ↔ `.cpp` 实现关系 | —— | 声明与定义成对 | Q01 |
| `ATTRIBUTE_ACCESSORS` 宏 | 属性集 | 批量生成属性的 getter/setter/initter | Q05 |
| `PROPERTY_GETTER` 宏 | 属性集 | 生成 `Get##属性##Attribute()` | Q05-1 |
| `VALUE_GETTER` 宏 | 属性集 | 生成读值 `Get##属性()` | Q05-2 |
| `VALUE_SETTER` 宏 | 属性集 | 生成 `Set##属性()`（走 ASC） | Q05-3 |
| `VALUE_INITTER` 宏 | 属性集 | 生成 `Init##属性()`（直接设初值） | Q05-4 |
| `FGameplayAttributeData` | GAS | 属性的底层数据载体 | Q07 |
| `GetLifetimeReplicatedProps` | `UObject`/`AActor` | 登记网络复制属性 | Q08 |
| `OnRep_Health` | 属性集 | 属性复制回调 | Q09 |
| `GetWorld()` | `UObject` | 获取所在世界 | Q02 |
| `GetTenkaichiAbilitySystemComponent()` | 属性集基类 | 拿到宿主上的 ASC | Q02/Q03 |

### 第 02 课 · 第 3 步（自定义 ASC）

| 方法/宏 | 所属 | 作用 | 详解 md |
|---------|------|------|---------|
| （自定义 ASC 类骨架） | `UAbilitySystemComponent` | 继承引擎 ASC 建项目 ASC 基类 | 02_3_1~3 |

### 第 02 课 · 第 4 步（自带 ASC 的角色类）

| 方法/宏 | 所属 | 作用 | 详解 md |
|---------|------|------|---------|
| `IAbilitySystemInterface` | GAS 接口 | 暴露 ASC 的接口，纯虚 `GetAbilitySystemComponent` | 02_4_2 |
| `GetAbilitySystemComponent()` | `IAbilitySystemInterface` | 返回角色用的 ASC | 02_4_2 |
| `PostInitializeComponents()` | `AActor` | 所有组件初始化完后的收尾回调 | Q11 |
| `InitAbilityActorInfo(Owner, Avatar)` | `UAbilitySystemComponent` | 关联 ASC 的宿主与 Avatar | 02_4_3 |
| `SetIsReplicated(bool)` | `UActorComponent` | 打开/关闭组件复制 | Q12 |
| `SetReplicationMode(EGameplayEffectReplicationMode)` | `UAbilitySystemComponent` | 设 GE 复制精细度（Mixed 等） | Q12 |
| `SetNetUpdateFrequency(float)` | `AActor` | 设角色每秒复制检查频率 | Q12 |
| `LyraCharacterWithAbilities`（何时用） | —— | self-contained ASC 范例类，NPC/教学骨架用 | Q10 |

---

## 三、待教（后续遇到再登记）

- `GiveAbility` / `AddAttributeSetSubobject` / `GetSet<T>`（授予属性集/技能，第 5 步起）
- `ApplyGameplayEffectToSelf` / GameplayEffect 相关
- 其余随课程推进登记

---

## 四、维护要求

1. **教完即登记**：每教一个新方法，同一轮就在上表补一行。
2. **教前查表**：代码里出现方法，先查本表是否已教。
3. **配合**：[18](./18_每次教学遇到没教过的方法都要教一遍.md)（本表就是它的落地工具）、[12](./12_教的每个属性方法都要核实存在_禁止凭脑子觉得有.md)（登记前确认源码位置真实）。
