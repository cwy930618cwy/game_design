# 06 — 统一路径约定（不再区分公司/家里）

> **定位**：本文件记录项目的**统一路径**。所有教学操作一律使用下表路径，不再区分公司/家里。
>
> **一句话**：只有一套路径，无论在哪儿开发都用同一套绝对路径。

---

## 一、统一路径表

| 资源 | 路径 |
|------|------|
| **Tenkaichi 项目** | `E:\ue5\game_design\code\Tenkaichi` |
| **Lyra 参考项目** | `E:\ue5\LyraStarterGame5.6\LyraStarterGame` |
| **UE5.6 引擎** | `D:\ue5\Epic Games\UE_5.6` |

---

## 二、workspace 文件

统一使用 `Tenkaichi.code-workspace`（项目根目录），包含三个文件夹：
- Tenkaichi（项目本身）
- LyraStarterGame（参考项目）
- UE5.6 引擎

---

## 三、执行要求

1. **所有文件操作**统一用上表路径，不需要询问用户当前位置。
2. **Lyra/引擎源码查阅**也用表中的固定路径。
3. **笔记目录**始终在 Tenkaichi 项目内的 `AI_Learning_Notes\` 下。
