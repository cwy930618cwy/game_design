# `LyraAbilitySystemGlobals.cpp` 速览

> 21 行，两个函数。

| 函数 | 干嘛的 |
|---|---|
| 构造函数 | 只调 Super |
| `AllocGameplayEffectContext()` | `return new FLyraGameplayEffectContext();` |

**说明**：整个文件就这一行有效代码，但它**影响全项目所有 GE**。
