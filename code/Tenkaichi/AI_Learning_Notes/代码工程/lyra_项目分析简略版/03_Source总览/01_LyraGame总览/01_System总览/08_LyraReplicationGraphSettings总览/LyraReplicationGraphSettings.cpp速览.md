# `LyraReplicationGraphSettings.cpp` 速览

> 11 行，只有构造函数。

| 步骤 | 干嘛的 |
|---|---|
| `CategoryName = TEXT("Game")` | 让它在项目设置里显示在 Game 分类下 |
| `DefaultReplicationGraphClass = ULyraReplicationGraph::StaticClass()` | 回填默认的 Graph 类 |
