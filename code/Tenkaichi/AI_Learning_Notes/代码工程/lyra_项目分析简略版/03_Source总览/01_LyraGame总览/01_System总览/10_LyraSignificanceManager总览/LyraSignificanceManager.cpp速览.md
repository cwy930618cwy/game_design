# `LyraSignificanceManager.cpp` 速览

> 7 行，**只有一个 `#include` 和一个宏，没有任何函数实现**。

| 内容 | 干嘛的 |
|---|---|
| `#include "LyraSignificanceManager.h"` | – |
| `UE_INLINE_GENERATED_CPP_BY_NAME(...)` | 让 UHT 反射代码内联到本文件 |

**说明**：整个文件没有逻辑，它是 `.h` 的空壳配套文件。
