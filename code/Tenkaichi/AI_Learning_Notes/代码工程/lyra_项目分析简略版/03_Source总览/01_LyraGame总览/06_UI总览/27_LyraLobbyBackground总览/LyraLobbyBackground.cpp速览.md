# `LyraLobbyBackground.cpp` 速览

> 8 行，**除了 include 和一个 UHT 宏，什么都没有**。

| 内容 | 干嘛的 |
|---|---|
| `#include "LyraLobbyBackground.h"` | – |
| `UE_INLINE_GENERATED_CPP_BY_NAME(...)` | UHT 配套宏 |

**说明**：这个文件完全是空的，所有内容都在 `.h` 里。
