# `IndicatorLibrary.cpp` 速览

> 20 行，两个函数。

| 函数 | 干嘛的 |
|---|---|
| 构造函数 | 空 |
| `GetIndicatorManagerComponent()` | 一行：转调 `ULyraIndicatorManagerComponent::GetComponent(Controller)` |

**说明**：纯转发，没有任何额外逻辑。
