# `LyraPawnData.cpp` 速览

> 15 行，**只有一个构造函数**。

| 步骤 | 干嘛的 |
|---|---|
| 构造函数 | 把 `PawnClass` / `InputConfig` / `DefaultCameraMode` 初始化为 nullptr |

**说明**：这个文件没有任何逻辑。Lyra 把它保留成独立的 cpp，只是为了配套 UHT 的 `UE_INLINE_GENERATED_CPP_BY_NAME`。
