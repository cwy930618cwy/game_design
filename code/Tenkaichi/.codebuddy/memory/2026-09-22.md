# 2026-09-22

## Tenkaichi 项目教程更新
- 将 `AI_Learning_Notes/Lyra_从零开发教学/` 目录下所有教程从旧项目名 `code`（路径 `e:\ue5\cyra\code`）更新为新项目 `Tenkaichi`（路径 `e:\ue5\game_design\code\Tenkaichi`）
- 类名前缀从 `Code` 全部改为 `Tenkaichi`（如 `UCodeAttributeSet` → `UTenkaichiAttributeSet`）
- 文件重命名：`01_教程设计总纲_贴合code项目.md` → `01_教程设计总纲_贴合Tenkaichi项目.md`
- 项目实际状态：UE5.6 空白C++工程，单模块 Tenkaichi，Build.cs 已含 EnhancedInput，GAS 尚未加
- 涉及修改文件：00a_按Lyra写法开发前的准备.md、01_教程设计总纲_贴合Tenkaichi项目.md、阶段一：GAS 打通/02_1_给工程加GAS依赖.md

## 教学铁律补充（重要，用户多次强调）
用户在教学中反复强调两条执行纪律，已固化为 `教学经验新对话必看/` 新文件：
- `07_每教一小步就立刻建md不等提醒.md`：拆细教学后，每教完一小步就在同一轮立刻建好对应小 md，不等/不问/不拖，用户催一次=AI失职一次。命名用下划线连写如 `02_2_1`。
- `08_教学md开头要先讲为什么再给代码.md`：给代码前必须先讲"这一小步解决什么问题、为什么这么写"（大白话+类比），代码靠后，贴完逐段回扣"为什么"。禁止上来丢代码让用户猜。
- 教训来源：第02课第2步（建属性集），AI 先只口头拆不建md（催两次才建）、又留了个塞5+知识点的大md（违反05）、且开场直接丢代码不讲为什么。
- 第02课第2步已拆成3小步并各建md：`02_2_1_属性集_建h骨架.md`（已补"为什么"开场）、`02_2_2_属性集_加Health属性.md`、`02_2_3_属性集_写cpp.md`。
- 教学进度：第02课第1步（GAS依赖）已完成（Build.cs加三模块+uproject启用插件，均已核对落地）。当前应教第2步第1小步（属性集.h骨架）。

## 教学铁律补充2（路径对应）
- `09_新建文件夹也要一比一对应Lyra目录.md`：让用户新建文件前，先到 LyraStarterGame5.6 查该类真实目录，Tenkaichi 路径一比一映射（Lyra的 AbilitySystem/Attributes/ → Tenkaichi的 AbilitySystem/Attributes/），禁止自创 Lyra 没有的顶层文件夹。
- 纠错：属性集正确路径是 `Source/Tenkaichi/AbilitySystem/Attributes/`，之前误写成 `Source/Tenkaichi/AttributeSet/`。已修正 02_2_1、02_2_3、01_教程设计总纲 三处。
- Lyra GAS 目录结构：AbilitySystem/Attributes/（属性集）、AbilitySystem/（ASC、AbilitySet）、AbilitySystem/Abilities/、Effects/、Executions/；Character/（角色）。

## 教学铁律补充3（代码文件归属也要对应Lyra）
- `10_代码放哪个文件也要对应Lyra禁止乱放.md`：不只文件夹，连"哪个类/哪段代码放哪个文件"也要一比一对应Lyra。禁止把Lyra分开的类擅自合并成一个文件"简化"，也禁止乱放。要简化必须先征求同意并说明会和Lyra哪里对不上。
- 纠错：之前把Health塞进基类TenkaichiAttributeSet一个文件（错误简化），已重构为Lyra真实两文件结构：基类TenkaichiAttributeSet（对应LyraAttributeSet，不含属性）+ 血量集TenkaichiHealthSet（对应LyraHealthSet，定义Health）。
- 第02课第2步现拆成4小步md：02_2_1基类.h、02_2_2基类.cpp、02_2_3血量集.h、02_2_4血量集.cpp。全部路径在 AbilitySystem/Attributes/。
- 用户核心诉求：一比一还原Lyra包括 文件夹+文件+代码组织 三个层面，任何"简化"都破坏对应关系，用户强烈反对。

## 教学疑问解答目录
- 新建目录 `AI_Learning_Notes/Lyra_从零开发教学/教学疑问解答/`，存放用户学习中的疑问解答。文件命名 `Q01_第X课第Y步_主题.md`，md 内必须说明"所属章节"。
- Q01：第02课第2步基类疑问——①基类构造函数为空是正常的（无属性要初始化，Lyra基类LyraAttributeSet.cpp也是空的）；②用户".h声明+.cpp实现"理解正确，是我教学md的.h漏了GetWorld/GetTenkaichiAbilitySystemComponent两个声明，已补上02_2_1的.h。
- 教训：给教学md代码时要确保 .h 声明与 .cpp 实现完整对应，别漏声明。

## 教学铁律补充4（给代码前两个核对）
- `11_给教学代码前必须做两个核对.md`：每次给教学代码前必须做两个核对——①对着Lyra真实源码逐行核有没有缺行/漏函数/漏声明；②.h声明与.cpp实现成对核（名字/参数/返回值/const/override完全一致）。禁止凭脑子"觉得该这样"就写。自检标准：用户照抄.h+.cpp能否一次编译过。
- 根因：漏声明是因为凭脑子写"最简骨架"，没逐行照抄Lyra源码。Lyra的LyraAttributeSet.h里明明有那两个声明，照抄就不会漏。

## 教学铁律补充5（每个符号都要核实存在）
- `12_教的每个属性方法都要核实存在_禁止凭脑子觉得有.md`：教学里讲的每个类/属性/方法/宏/函数/include，教之前都要到LyraStarterGame5.6或UE_5.6源码搜到、读到、确认存在+签名，能指出"在哪个文件第几行"。禁止凭脑子"应该有这个"就教。记忆/常识会骗人，一律以源码为准。
- 完整核对链条：核对③(12,符号真实存在)→核对①(11,照Lyra没漏)→核对②(11,.h/.cpp成对)。
- 至此"教学经验新对话必看"目录已有 00~12 共13个铁律文件。

## 按铁律11/12逐行核对教学（发现并修正真实bug）
- 对照Lyra真实 LyraAttributeSet.h/LyraHealthSet.h 逐行核对 02_2_1~02_2_4 四个教学md。
- 发现bug：02_2_1基类.h代码漏了前向声明 class UWorld; / class UTenkaichiAbilitySystemComponent;（Lyra的LyraAttributeSet.h第11-15行有），.h里用它们做指针返回值会编译报错。已补上。这正是"没照抄Lyra、凭脑子觉得够"导致的漏，印证铁律11/12。
- 另修02_2_2标题措辞：GetLyraAbilitySystemComponent→GetTenkaichiAbilitySystemComponent（对应Lyra的GetLyraAbilitySystemComponent）。
- 核对结论：血量集02_2_3/.h的FGameplayAttributeData经基类.h传递include可用；Health的HideFromModifiers已标注教学简化。4个md现均与Lyra一一对应、.h/.cpp成对、符号真实存在。

## 教学疑问解答Q02（用剧场故事讲解基类工具函数）
- `教学疑问解答/Q02_第02课第2步_基类两个工具函数_使用场景与故事.md`：解答GetWorld/GetTenkaichiAbilitySystemComponent用途。核心：属性集是"死数据表"不知自己在哪/归谁管，GetWorld顺Outer摸到World，GetASC用GetOwningAbilitySystemComponent拿通用ASC再Cast成项目自己的类型。
- 用户偏好：喜欢用剧场故事/类比讲解（人物谱世界观：演员=角色、技能背包=ASC、血型档案卡=属性集）。已用此风格编故事。
- 用户人物谱目录：AI_Learning_Notes/人物介绍/（01启动、02玩家角色、03运行时链路、04输入、05UI）。
