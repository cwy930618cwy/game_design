# `LyraGameViewportClient.h` 速览

> 22 行，**只 override 了一个函数**。

| 成员 | 干嘛的 |
|---|---|
| `: UCommonGameViewportClient`（`BlueprintType`） | 继承 CommonGame 插件的版本 |
| `Init()` | 唯一的 override |

**说明**：看 cpp 才知道它干嘛 —— 决定用软件光标还是硬件光标。
