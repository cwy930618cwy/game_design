# `LyraSignificanceManager.h` 速览

> 11 行，**一个完全空的类**。

| 成员 | 干嘛的 |
|---|---|
| `UCLASS()` `ULyraSignificanceManager : USignificanceManager` | 空类体，没有任何成员 |

**说明**：Lyra 继承了 `USignificanceManager` 但没有往里加东西，只是为了让项目里能用这个具体类型（方便将来扩展）。
